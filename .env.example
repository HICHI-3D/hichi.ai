# 추론 서버
APP_ENV=development
APP_PORT=8001

# 모델 경로
YOLO_WEIGHTS=models/weights/yolov8n.pt
SAM_WEIGHTS=models/weights/sam_vit_b.pth

# 디바이스 (cpu / cuda / mps)
DEVICE=mps

# 외부 도구 경로 (시스템에 설치 필요)
COLMAP_BIN=colmap
OPENMVS_BIN_DIR=/usr/local/bin
MESHLAB_BIN=meshlabserver

# 작업 디렉터리
WORK_DIR=./outputs
